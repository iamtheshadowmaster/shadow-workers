---
import shadowIcon from '../assets/128x128.png';
import gmailIcon from '../assets/gmail.png';
import notionIcon from '../assets/notion.png';
import calendarIcon from '../assets/googlecalendar.png';

const shadowIconSrc = shadowIcon.src;
const gmailIconSrc = gmailIcon.src;
const notionIconSrc = notionIcon.src;
const calendarIconSrc = calendarIcon.src;
---

<!-- Scroll container that creates the scroll distance -->
<div id="plan-section" class="relative bg-black" style="height: 250vh;" data-shadow-icon={shadowIconSrc} data-gmail-icon={gmailIconSrc} data-notion-icon={notionIconSrc} data-calendar-icon={calendarIconSrc}>
  <!-- Fixed/sticky content -->
  <div id="plan-sticky" class="sticky top-0 min-h-screen flex items-center bg-black py-16">
    <div class="max-w-7xl mx-auto px-6 w-full">
      <!-- Title -->
      <div class="text-center mb-12">
        <div class="inline-flex items-center gap-2 mb-6">
          <div class="w-2 h-2 rounded-full bg-blue-500"></div>
          <span class="text-sm font-medium text-blue-400 uppercase tracking-wider">Solo Mode</span>
        </div>
        <h2 class="text-xl md:text-3xl lg:text-4xl font-medium text-white mb-6 leading-tight tracking-tight">
          Zero setup. Instant value. Daily progress.
        </h2>
      </div>

      <div class="flex items-center gap-12">
        <!-- Left: Content -->
        <div class="flex-1 flex flex-col justify-between">
          <div id="step-content" class="transition-opacity duration-500">
            <!-- Step content will be dynamically shown here -->
          </div>

          <!-- Progress Indicator at bottom -->
          <div class="mt-12">
            <div class="flex items-center">
              <div class="step-indicator flex items-center" data-step="1">
                <div class="step-dot w-3 h-3 rounded-full transition-all duration-300 bg-white border-2 border-gray-600"></div>
                <div class="step-line h-0.5 w-12 bg-gray-800 transition-all duration-300"></div>
              </div>
              <div class="step-indicator flex items-center" data-step="2">
                <div class="step-dot w-3 h-3 rounded-full transition-all duration-300 bg-white border-2 border-gray-600"></div>
                <div class="step-line h-0.5 w-12 bg-gray-800 transition-all duration-300"></div>
              </div>
              <div class="step-indicator flex items-center" data-step="3">
                <div class="step-dot w-3 h-3 rounded-full transition-all duration-300 bg-white border-2 border-gray-600"></div>
                <div class="step-line h-0.5 w-12 bg-gray-800 transition-all duration-300"></div>
              </div>
              <div class="step-indicator flex items-center" data-step="4">
                <div class="step-dot w-3 h-3 rounded-full transition-all duration-300 bg-white border-2 border-gray-600"></div>
              </div>
            </div>
          </div>
        </div>

        <!-- Right: Screenshot -->
        <div class="flex-1 hidden lg:block">
          <div id="step-image" class="rounded-2xl bg-gradient-to-br from-purple-500/20 to-blue-500/20 aspect-[4/3] overflow-hidden transition-all duration-500">
            <!-- Screenshot will be shown here -->
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  // Get the icon paths from the data attributes
  const shadowIconSrc = document.getElementById('plan-section')?.getAttribute('data-shadow-icon') || '';
  const gmailIconSrc = document.getElementById('plan-section')?.getAttribute('data-gmail-icon') || '';
  const notionIconSrc = document.getElementById('plan-section')?.getAttribute('data-notion-icon') || '';
  const calendarIconSrc = document.getElementById('plan-section')?.getAttribute('data-calendar-icon') || '';

  const steps = [
    {
      number: 1,
      title: "Instant onboarding",
      description: "Give Shadow your role, company URL and current biggest goal. Shadow starts gathering a comprehensive context, better than you ever would.",
      customRender: 'chat'
    },
    {
      number: 2,
      title: "Personal roadmap",
      description: "Shadow creates your personal system prompt—capturing your role, company context, and objectives into a strategic blueprint that persists across every conversation.",
      customRender: 'systemPrompt'
    },
    {
      number: 3,
      title: "Daily reality check",
      description: "Every morning, Shadow asks one targeted question about your goals. It's like having your smartest colleague tap you on the shoulder.",
      customRender: 'slackNotification'
    },
    {
      number: 4,
      title: "Plug your tools",
      description: "Connect Slack, Calendar, Gmail, Google Drive, Notion, GitHub, Salesforce, Linear and more so Shadow can read your patterns and help you work better.",
      customRender: 'integrations'
    }
  ];

  let currentStep = 0;

  function renderStep(index: number) {
    const step = steps[index];
    const contentDiv = document.getElementById('step-content');
    const imageDiv = document.getElementById('step-image');

    if (contentDiv) {
      contentDiv.innerHTML = `
        <div class="mb-4">
          <span class="text-sm font-medium text-gray-500">${step.number}/4</span>
        </div>
        <h3 class="text-3xl md:text-4xl font-bold text-white mb-6">
          ${step.title}
        </h3>
        <p class="text-xl text-gray-400 leading-relaxed max-w-xl">
          ${step.description}
        </p>
      `;
    }

    if (imageDiv) {
      if (step.customRender === 'chat') {
        imageDiv.innerHTML = `
          <div class="rounded-xl bg-[#1a1a2e] p-5 h-full flex items-center justify-center shadow-2xl relative overflow-hidden border border-gray-700/30">
            <!-- Multi-color gradient overlay effects -->
            <div class="absolute inset-0 bg-gradient-to-br from-yellow-600/20 via-purple-600/20 to-cyan-500/20 pointer-events-none"></div>
            <div class="absolute top-0 left-0 w-96 h-96 bg-yellow-500/15 rounded-full blur-3xl pointer-events-none"></div>
            <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[500px] h-[500px] bg-purple-600/20 rounded-full blur-3xl pointer-events-none"></div>
            <div class="absolute bottom-0 right-0 w-96 h-96 bg-cyan-500/15 rounded-full blur-3xl pointer-events-none"></div>
            <div class="absolute top-0 right-0 w-80 h-80 bg-blue-600/10 rounded-full blur-3xl pointer-events-none"></div>

            <!-- Content wrapper -->
            <div class="relative z-10 flex flex-col w-full max-w-lg">
            <!-- First Message Block -->
            <div class="mb-5">
              <!-- Avatar and Header -->
              <div class="flex items-start gap-3 mb-3">
                <div class="w-9 h-9 rounded-lg overflow-hidden flex items-center justify-center flex-shrink-0 bg-white/5">
                  <img src="${shadowIconSrc}" alt="Shadow" class="w-full h-full object-cover" />
                </div>
                <div class="flex-1">
                  <div class="flex items-center gap-2 mb-1">
                    <span class="text-white font-semibold text-sm">Shadow</span>
                    <span class="text-xs text-gray-500">about 1 min ago</span>
                  </div>
                  <div class="text-[13px] text-gray-300 leading-relaxed">
                    Hey <span class="text-white font-medium">Sarah</span>, welcome on board! Let me gather some context about Acme.
                  </div>
                </div>
              </div>

              <!-- Collapsible Status Card -->
              <div class="ml-12 mt-3">
                <div class="bg-[#35354a] rounded-lg border border-gray-700/50">
                  <!-- Header -->
                  <div class="px-3 py-2.5 flex items-center justify-between cursor-pointer hover:bg-gray-700/20 transition-colors">
                    <div class="flex items-center gap-2">
                      <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                      </svg>
                      <span class="text-xs font-medium text-gray-300">Completed</span>
                    </div>
                    <div class="flex items-center gap-3">
                      <span class="text-xs text-gray-500">5 steps</span>
                      <svg class="w-3.5 h-3.5 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                      </svg>
                    </div>
                  </div>

                  <!-- Task Item -->
                  <div class="px-3 py-2.5 border-t border-gray-700/50">
                    <div class="flex items-start gap-2.5">
                      <svg class="w-4 h-4 text-gray-400 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9a9 9 0 01-9-9m9 9c1.657 0 3-4.03 3-9s-1.343-9-3-9m0 18c-1.657 0-3-4.03-3-9s1.343-9 3-9m-9 9a9 9 0 019-9"></path>
                      </svg>
                      <div class="flex-1 min-w-0">
                        <div class="text-[13px] text-gray-200 mb-1">Acme product strategy on Generative AI</div>
                        <div class="flex items-center gap-2">
                          <span class="text-xs text-blue-400">7 results</span>
                          <svg class="w-3 h-3 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                          </svg>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>

                <!-- Second Collapsible Card -->
                <div class="bg-[#35354a] rounded-lg border border-gray-700/50 mt-2">
                  <div class="px-3 py-2.5 flex items-center justify-between cursor-pointer hover:bg-gray-700/20 transition-colors">
                    <div class="flex items-center gap-2">
                      <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                      </svg>
                      <span class="text-xs font-medium text-gray-300">Reasoning completed</span>
                    </div>
                    <svg class="w-3.5 h-3.5 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                    </svg>
                  </div>
                </div>
              </div>
            </div>

            <!-- Second Message Block -->
            <div>
              <div class="flex items-start gap-3">
                <div class="w-9 h-9 rounded-lg overflow-hidden flex items-center justify-center flex-shrink-0 bg-white/5">
                  <img src="${shadowIconSrc}" alt="Shadow" class="w-full h-full object-cover" />
                </div>
                <div class="flex-1">
                  <div class="flex items-center gap-2 mb-2">
                    <span class="text-white font-semibold text-sm">Shadow</span>
                    <span class="text-xs text-gray-500">Just now</span>
                  </div>
                  <div class="text-[13px] text-gray-300 leading-relaxed">
                    I see there's a lot going on at <span class="text-white font-medium">Acme</span> right now, especially with the push around <span class="text-purple-400 font-medium">generative AI</span> and the recent acquisition of LucentAI. As a Product Manager, that probably has some impact on your <span class="text-blue-400 font-medium">roadmap</span>. I went ahead and prepared a few preliminary tasks, and also drafted a first version of your own system prompt to help move things along.
                  </div>
                </div>
              </div>
            </div>


            </div>
          </div>
        `;
      } else if (step.customRender === 'systemPrompt') {
        imageDiv.innerHTML = `
          <div class="rounded-xl bg-[#1a1a2e] p-5 h-full flex items-center justify-center shadow-2xl relative overflow-hidden border border-gray-700/30">
            <!-- Multi-color gradient overlay effects -->
            <div class="absolute inset-0 bg-gradient-to-br from-yellow-600/20 via-purple-600/20 to-cyan-500/20 pointer-events-none"></div>
            <div class="absolute top-0 left-0 w-96 h-96 bg-yellow-500/15 rounded-full blur-3xl pointer-events-none"></div>
            <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[500px] h-[500px] bg-purple-600/20 rounded-full blur-3xl pointer-events-none"></div>
            <div class="absolute bottom-0 right-0 w-96 h-96 bg-cyan-500/15 rounded-full blur-3xl pointer-events-none"></div>
            <div class="absolute top-0 right-0 w-80 h-80 bg-blue-600/10 rounded-full blur-3xl pointer-events-none"></div>

            <!-- Content wrapper -->
            <div class="relative z-10 flex flex-col w-full max-w-xl">
              <!-- Modal Card -->
              <div class="bg-[#2a2a35] rounded-xl border border-gray-700/50 overflow-hidden">
                <!-- Header -->
                <div class="px-4 py-3 border-b border-gray-700/50 flex items-center justify-between">
                  <div class="flex items-center gap-2.5">
                    <div class="w-7 h-7 rounded-lg overflow-hidden flex items-center justify-center bg-white/5">
                      <img src="${shadowIconSrc}" alt="Shadow" class="w-full h-full object-cover" />
                    </div>
                    <div>
                      <div class="text-white font-semibold text-xs">Your Strategic Blueprint</div>
                      <div class="text-[10px] text-gray-400">Generated by Shadow</div>
                    </div>
                  </div>
                  <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                  </svg>
                </div>

                <!-- Content -->
                <div class="px-4 py-4">
                  <div class="space-y-3">
                    <div class="text-base font-semibold text-white leading-snug">
                      You're a Product Manager juggling 5 features across 3 teams.
                    </div>
                    <div class="text-base font-semibold text-white leading-snug">
                      Your goal: Ship the activation improvement feature on schedule while keeping Engineering aligned.
                    </div>
                    <div class="text-base font-semibold text-white leading-snug">
                      Shadow helps you know <span class="text-purple-400">what to ship first</span> and keeps your squad pulling toward the same OKR.
                    </div>
                  </div>
                </div>

                <!-- Footer -->
                <div class="px-4 py-3 bg-[#35354a] border-t border-gray-700/50">
                  <div class="flex items-center justify-between">
                    <div class="text-[10px] text-gray-400">This prompt persists across every conversation</div>
                    <button class="px-3 py-1.5 bg-purple-600 hover:bg-purple-700 text-white text-xs font-medium rounded-lg transition-colors">
                      Save & Continue
                    </button>
                  </div>
                </div>
              </div>
            </div>
          </div>
        `;
      } else if (step.customRender === 'slackNotification') {
        imageDiv.innerHTML = `
          <div class="rounded-xl bg-[#1a1a2e] p-5 h-full flex items-center justify-center shadow-2xl relative overflow-hidden border border-gray-700/30">
            <!-- Multi-color gradient overlay effects -->
            <div class="absolute inset-0 bg-gradient-to-br from-yellow-600/20 via-purple-600/20 to-cyan-500/20 pointer-events-none"></div>
            <div class="absolute top-0 left-0 w-96 h-96 bg-yellow-500/15 rounded-full blur-3xl pointer-events-none"></div>
            <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[500px] h-[500px] bg-purple-600/20 rounded-full blur-3xl pointer-events-none"></div>
            <div class="absolute bottom-0 right-0 w-96 h-96 bg-cyan-500/15 rounded-full blur-3xl pointer-events-none"></div>
            <div class="absolute top-0 right-0 w-80 h-80 bg-blue-600/10 rounded-full blur-3xl pointer-events-none"></div>

            <!-- Content wrapper -->
            <div class="relative z-10 flex flex-col w-full max-w-lg">
              <!-- Slack Notification -->
              <div class="bg-white rounded-lg shadow-xl overflow-hidden">
                <!-- Slack Header -->
                <div class="bg-[#4a154b] px-4 py-3 flex items-center gap-3">
                  <svg class="w-6 h-6 text-white" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M6 15a2 2 0 0 1-2 2a2 2 0 0 1-2-2a2 2 0 0 1 2-2h2v2zm1 0a2 2 0 0 1 2-2a2 2 0 0 1 2 2v5a2 2 0 0 1-2 2a2 2 0 0 1-2-2v-5zM9 6a2 2 0 0 1-2-2a2 2 0 0 1 2-2a2 2 0 0 1 2 2v2H9zm0 1a2 2 0 0 1 2 2a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2a2 2 0 0 1 2-2h5zm9 2a2 2 0 0 1 2-2a2 2 0 0 1 2 2a2 2 0 0 1-2 2h-2V9zm-1 0a2 2 0 0 1-2 2a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2a2 2 0 0 1 2 2v5zm-2 9a2 2 0 0 1 2 2a2 2 0 0 1-2 2a2 2 0 0 1-2-2v-2h2zm0-1a2 2 0 0 1-2-2a2 2 0 0 1 2-2h5a2 2 0 0 1 2 2a2 2 0 0 1-2 2h-5z"/>
                  </svg>
                  <div class="flex-1">
                    <div class="text-white font-bold text-sm">Shadow</div>
                    <div class="text-gray-300 text-xs">8:00 AM</div>
                  </div>
                </div>

                <!-- Message Content -->
                <div class="p-4">
                  <div class="flex items-start gap-3">
                    <div class="w-10 h-10 rounded overflow-hidden flex-shrink-0 bg-gradient-to-br from-purple-600 to-blue-600">
                      <img src="${shadowIconSrc}" alt="Shadow" class="w-full h-full object-cover" />
                    </div>
                    <div class="flex-1">
                      <div class="flex items-baseline gap-2 mb-1">
                        <span class="font-bold text-[#1d1c1d] text-sm">Shadow</span>
                        <span class="text-xs text-gray-500">APP</span>
                        <span class="text-xs text-gray-500">8:00 AM</span>
                      </div>
                      <div class="text-[#1d1c1d] text-[15px] leading-relaxed">
                        Good morning! 👋
                        <br/><br/>
                        Quick reality check: Your activation feature launch is in 2 weeks. Engineering is asking for clearer specs on the onboarding flow.
                        <br/><br/>
                        <strong>What's your top priority today to keep this on track?</strong>
                      </div>
                      <div class="mt-3 flex gap-2">
                        <button class="px-3 py-1.5 bg-white border-2 border-gray-300 text-[#1d1c1d] text-sm font-medium rounded hover:bg-gray-50 transition-colors">
                          Finalize specs
                        </button>
                        <button class="px-3 py-1.5 bg-white border-2 border-gray-300 text-[#1d1c1d] text-sm font-medium rounded hover:bg-gray-50 transition-colors">
                          Sync with Engineering
                        </button>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        `;
      } else if (step.customRender === 'integrations') {
        imageDiv.innerHTML = `
          <div class="rounded-xl bg-[#1a1a2e] p-5 h-full flex items-center justify-center shadow-2xl relative overflow-hidden border border-gray-700/30">
            <!-- Multi-color gradient overlay effects -->
            <div class="absolute inset-0 bg-gradient-to-br from-yellow-600/20 via-purple-600/20 to-cyan-500/20 pointer-events-none"></div>
            <div class="absolute top-0 left-0 w-96 h-96 bg-yellow-500/15 rounded-full blur-3xl pointer-events-none"></div>
            <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[500px] h-[500px] bg-purple-600/20 rounded-full blur-3xl pointer-events-none"></div>
            <div class="absolute bottom-0 right-0 w-96 h-96 bg-cyan-500/15 rounded-full blur-3xl pointer-events-none"></div>
            <div class="absolute top-0 right-0 w-80 h-80 bg-blue-600/10 rounded-full blur-3xl pointer-events-none"></div>

            <!-- Content wrapper -->
            <div class="relative z-10 flex flex-col w-full max-w-2xl">
              <!-- Integrations Panel -->
              <div class="bg-[#2a2a35] rounded-xl border border-gray-700/50 overflow-hidden">
                <!-- Header -->
                <div class="px-6 py-4 border-b border-gray-700/50">
                  <h3 class="text-white font-semibold text-lg">Connect Your Tools</h3>
                  <p class="text-gray-400 text-sm mt-1">Shadow reads your patterns and helps you work better</p>
                </div>

                <!-- Integration Grid -->
                <div class="p-6">
                  <div class="grid grid-cols-3 gap-4">
                    <!-- Calendar -->
                    <div class="bg-[#35354a] rounded-lg p-4 border border-gray-700/50 hover:border-purple-500/50 transition-all cursor-pointer group">
                      <div class="w-12 h-12 bg-white rounded-lg flex items-center justify-center mb-3 mx-auto overflow-hidden p-1.5">
                        <img src="${calendarIconSrc}" alt="Google Calendar" class="w-full h-full object-contain" />
                      </div>
                      <div class="text-center">
                        <div class="text-white font-medium text-sm mb-1">Calendar</div>
                        <div class="text-xs text-gray-400">Google, Outlook</div>
                      </div>
                      <div class="mt-3 flex justify-center">
                        <div class="px-3 py-1 bg-purple-600 text-white text-xs font-medium rounded group-hover:bg-purple-700 transition-colors">
                          Connect
                        </div>
                      </div>
                    </div>

                    <!-- Gmail -->
                    <div class="bg-[#35354a] rounded-lg p-4 border border-gray-700/50 hover:border-purple-500/50 transition-all cursor-pointer group">
                      <div class="w-12 h-12 bg-white rounded-lg flex items-center justify-center mb-3 mx-auto overflow-hidden p-1.5">
                        <img src="${gmailIconSrc}" alt="Gmail" class="w-full h-full object-contain" />
                      </div>
                      <div class="text-center">
                        <div class="text-white font-medium text-sm mb-1">Gmail</div>
                        <div class="text-xs text-gray-400">Email triage</div>
                      </div>
                      <div class="mt-3 flex justify-center">
                        <div class="px-3 py-1 bg-purple-600 text-white text-xs font-medium rounded group-hover:bg-purple-700 transition-colors">
                          Connect
                        </div>
                      </div>
                    </div>

                    <!-- Notion -->
                    <div class="bg-[#35354a] rounded-lg p-4 border border-gray-700/50 hover:border-purple-500/50 transition-all cursor-pointer group">
                      <div class="w-12 h-12 bg-white rounded-lg flex items-center justify-center mb-3 mx-auto overflow-hidden p-1.5">
                        <img src="${notionIconSrc}" alt="Notion" class="w-full h-full object-contain" />
                      </div>
                      <div class="text-center">
                        <div class="text-white font-medium text-sm mb-1">Notion</div>
                        <div class="text-xs text-gray-400">Docs & wikis</div>
                      </div>
                      <div class="mt-3 flex justify-center">
                        <div class="px-3 py-1 bg-purple-600 text-white text-xs font-medium rounded group-hover:bg-purple-700 transition-colors">
                          Connect
                        </div>
                      </div>
                    </div>
                  </div>

                  <!-- More integrations hint -->
                  <div class="mt-6 text-center">
                    <div class="text-sm text-gray-400">+ Slack, GitHub, Linear, Salesforce, and more</div>
                  </div>
                </div>

                <!-- Footer -->
                <div class="px-6 py-4 bg-[#35354a] border-t border-gray-700/50 flex items-center justify-between">
                  <div class="text-xs text-gray-400">All connections are encrypted and secure</div>
                  <button class="px-4 py-2 bg-gray-700 hover:bg-gray-600 text-white text-sm font-medium rounded-lg transition-colors">
                    Skip for now
                  </button>
                </div>
              </div>
            </div>
          </div>
        `;
      }
    }

    // Update progress indicators
    document.querySelectorAll('.step-indicator').forEach((indicator, i) => {
      const stepDot = indicator.querySelector('.step-dot');
      const stepLine = indicator.querySelector('.step-line');

      if (i < index) {
        // Completed steps
        stepDot?.classList.remove('bg-white', 'border-gray-600');
        stepDot?.classList.add('bg-black', 'border-white');
        stepLine?.classList.remove('bg-gray-800');
        stepLine?.classList.add('bg-white');
      } else if (i === index) {
        // Current step
        stepDot?.classList.remove('bg-white', 'border-gray-600');
        stepDot?.classList.add('bg-black', 'border-white');
        stepLine?.classList.remove('bg-white');
        stepLine?.classList.add('bg-gray-800');
      } else {
        // Future steps
        stepDot?.classList.remove('bg-black', 'border-white');
        stepDot?.classList.add('bg-white', 'border-gray-600');
        stepLine?.classList.remove('bg-white');
        stepLine?.classList.add('bg-gray-800');
      }
    });
  }

  function updateStepBasedOnScroll() {
    const section = document.getElementById('plan-section') as HTMLElement;
    if (!section) return;

    const rect = section.getBoundingClientRect();
    const sectionHeight = section.offsetHeight;
    const viewportHeight = window.innerHeight;

    // Calculate scroll progress through the section (0 to 1)
    const scrollProgress = Math.max(0, Math.min(1, -rect.top / (sectionHeight - viewportHeight)));

    // Determine which step to show (0-3)
    const newStep = Math.min(3, Math.floor(scrollProgress * 4));

    // Update step
    if (newStep !== currentStep) {
      currentStep = newStep;
      renderStep(currentStep);
    }
  }

  // Initialize
  renderStep(0);

  // Update on scroll
  window.addEventListener('scroll', updateStepBasedOnScroll);
  // Initial update
  updateStepBasedOnScroll();
</script>
