---
import shadowIcon from '../../assets/128x128.png';

const shadowIconSrc = shadowIcon.src;
---

<style>
  @keyframes float-1 {
    0%, 100% { transform: translate(0, 0) scale(1); }
    33% { transform: translate(30px, -30px) scale(1.1); }
    66% { transform: translate(-20px, 20px) scale(0.9); }
  }

  @keyframes float-2 {
    0%, 100% { transform: translate(-50%, -50%) scale(1); }
    33% { transform: translate(-45%, -55%) scale(1.15); }
    66% { transform: translate(-55%, -45%) scale(0.85); }
  }

  @keyframes float-3 {
    0%, 100% { transform: translate(0, 0) scale(1); }
    33% { transform: translate(-25px, 35px) scale(0.9); }
    66% { transform: translate(25px, -25px) scale(1.1); }
  }

  @keyframes float-4 {
    0%, 100% { transform: translate(0, 0) scale(1); }
    33% { transform: translate(20px, 20px) scale(1.05); }
    66% { transform: translate(-30px, -15px) scale(0.95); }
  }

  .animate-float-1 { animation: float-1 20s ease-in-out infinite; }
  .animate-float-2 { animation: float-2 25s ease-in-out infinite; }
  .animate-float-3 { animation: float-3 22s ease-in-out infinite; }
  .animate-float-4 { animation: float-4 18s ease-in-out infinite; }
</style>

<div class="rounded-xl bg-[#1a1a2e] p-5 h-full flex items-center justify-center shadow-2xl relative overflow-hidden border border-gray-700/30">
  <!-- Multi-color gradient overlay effects -->
  <div class="absolute inset-0 bg-gradient-to-br from-blue-600/20 via-indigo-600/20 to-purple-500/20 pointer-events-none"></div>
  <div class="absolute top-0 left-0 w-96 h-96 bg-blue-500/15 rounded-full blur-3xl pointer-events-none animate-float-1"></div>
  <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[500px] h-[500px] bg-indigo-600/20 rounded-full blur-3xl pointer-events-none animate-float-2"></div>
  <div class="absolute bottom-0 right-0 w-96 h-96 bg-purple-500/15 rounded-full blur-3xl pointer-events-none animate-float-3"></div>
  <div class="absolute top-0 right-0 w-80 h-80 bg-violet-600/10 rounded-full blur-3xl pointer-events-none animate-float-4"></div>

  <!-- Content wrapper -->
  <div class="relative z-10 flex flex-col w-full max-w-lg">
    <!-- First Message Block -->
    <div class="mb-5">
      <!-- Avatar and Header -->
      <div class="flex items-start gap-3 mb-3">
        <div class="w-9 h-9 rounded-lg overflow-hidden flex items-center justify-center flex-shrink-0 bg-white/5">
          <img src={shadowIconSrc} alt="Shadow" class="w-full h-full object-cover" />
        </div>
        <div class="flex-1">
          <div class="flex items-center gap-2 mb-1">
            <span class="text-white font-semibold text-sm">Shadow</span>
            <span class="text-xs text-gray-500">about 1 min ago</span>
          </div>
          <div class="text-[13px] text-gray-300 leading-relaxed">
            Hey <span class="text-white font-medium">Sarah</span>, welcome on board! Let me gather some context about Acme.
          </div>
        </div>
      </div>

      <!-- Collapsible Status Card -->
      <div class="ml-12 mt-3">
        <div class="bg-[#35354a] rounded-lg border border-gray-700/50">
          <!-- Header -->
          <div class="px-3 py-2.5 flex items-center justify-between cursor-pointer hover:bg-gray-700/20 transition-colors">
            <div class="flex items-center gap-2">
              <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
              </svg>
              <span class="text-xs font-medium text-gray-300">Completed</span>
            </div>
            <div class="flex items-center gap-3">
              <span class="text-xs text-gray-500">5 steps</span>
              <svg class="w-3.5 h-3.5 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
              </svg>
            </div>
          </div>

          <!-- Task Item -->
          <div class="px-3 py-2.5 border-t border-gray-700/50">
            <div class="flex items-start gap-2.5">
              <svg class="w-4 h-4 text-gray-400 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9a9 9 0 01-9-9m9 9c1.657 0 3-4.03 3-9s-1.343-9-3-9m0 18c-1.657 0-3-4.03-3-9s1.343-9 3-9m-9 9a9 9 0 019-9"></path>
              </svg>
              <div class="flex-1 min-w-0">
                <div class="text-[13px] text-gray-200 mb-1">Acme product strategy on Generative AI</div>
                <div class="flex items-center gap-2">
                  <span class="text-xs text-blue-400">7 results</span>
                  <svg class="w-3 h-3 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                  </svg>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Second Collapsible Card -->
        <div class="bg-[#35354a] rounded-lg border border-gray-700/50 mt-2">
          <div class="px-3 py-2.5 flex items-center justify-between cursor-pointer hover:bg-gray-700/20 transition-colors">
            <div class="flex items-center gap-2">
              <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
              </svg>
              <span class="text-xs font-medium text-gray-300">Reasoning completed</span>
            </div>
            <svg class="w-3.5 h-3.5 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
            </svg>
          </div>
        </div>
      </div>
    </div>

    <!-- Second Message Block -->
    <div>
      <div class="flex items-start gap-3">
        <div class="w-9 h-9 rounded-lg overflow-hidden flex items-center justify-center flex-shrink-0 bg-white/5">
          <img src={shadowIconSrc} alt="Shadow" class="w-full h-full object-cover" />
        </div>
        <div class="flex-1">
          <div class="flex items-center gap-2 mb-2">
            <span class="text-white font-semibold text-sm">Shadow</span>
            <span class="text-xs text-gray-500">Just now</span>
          </div>
          <div class="text-[13px] text-gray-300 leading-relaxed">
            I see there's a lot going on at <span class="text-white font-medium">Acme</span> right now, especially with the push around <span class="text-purple-400 font-medium">generative AI</span> and the recent acquisition of LucentAI. As a Product Manager, that probably has some impact on your <span class="text-blue-400 font-medium">roadmap</span>. I went ahead and prepared a few preliminary tasks, and also drafted a first version of your own system prompt to help move things along.
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
