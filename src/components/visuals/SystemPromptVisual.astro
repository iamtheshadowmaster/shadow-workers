---
import shadowIcon from '../../assets/128x128.png';

const shadowIconSrc = shadowIcon.src;
---

<style>
  @keyframes float-1 {
    0%, 100% { transform: translate(0, 0) scale(1); }
    33% { transform: translate(30px, -30px) scale(1.1); }
    66% { transform: translate(-20px, 20px) scale(0.9); }
  }

  @keyframes float-2 {
    0%, 100% { transform: translate(-50%, -50%) scale(1); }
    33% { transform: translate(-45%, -55%) scale(1.15); }
    66% { transform: translate(-55%, -45%) scale(0.85); }
  }

  @keyframes float-3 {
    0%, 100% { transform: translate(0, 0) scale(1); }
    33% { transform: translate(-25px, 35px) scale(0.9); }
    66% { transform: translate(25px, -25px) scale(1.1); }
  }

  @keyframes float-4 {
    0%, 100% { transform: translate(0, 0) scale(1); }
    33% { transform: translate(20px, 20px) scale(1.05); }
    66% { transform: translate(-30px, -15px) scale(0.95); }
  }

  .animate-float-1 { animation: float-1 20s ease-in-out infinite; }
  .animate-float-2 { animation: float-2 25s ease-in-out infinite; }
  .animate-float-3 { animation: float-3 22s ease-in-out infinite; }
  .animate-float-4 { animation: float-4 18s ease-in-out infinite; }
</style>

<div class="rounded-xl bg-[#1a1a2e] p-5 h-full flex items-center justify-center shadow-2xl relative overflow-hidden border border-gray-700/30">
  <!-- Multi-color gradient overlay effects -->
  <div class="absolute inset-0 bg-gradient-to-br from-emerald-600/20 via-teal-600/20 to-cyan-500/20 pointer-events-none"></div>
  <div class="absolute top-0 left-0 w-96 h-96 bg-emerald-500/15 rounded-full blur-3xl pointer-events-none animate-float-1"></div>
  <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[500px] h-[500px] bg-teal-600/20 rounded-full blur-3xl pointer-events-none animate-float-2"></div>
  <div class="absolute bottom-0 right-0 w-96 h-96 bg-cyan-500/15 rounded-full blur-3xl pointer-events-none animate-float-3"></div>
  <div class="absolute top-0 right-0 w-80 h-80 bg-teal-700/10 rounded-full blur-3xl pointer-events-none animate-float-4"></div>

  <!-- Content wrapper -->
  <div class="relative z-10 flex flex-col w-full max-w-xl">
    <!-- Modal Card -->
    <div class="bg-[#2a2a35] rounded-xl border border-gray-700/50 overflow-hidden">
      <!-- Header -->
      <div class="px-4 py-3 border-b border-gray-700/50 flex items-center justify-between">
        <div class="flex items-center gap-2.5">
          <div class="w-7 h-7 rounded-lg overflow-hidden flex items-center justify-center bg-white/5">
            <img src={shadowIconSrc} alt="Shadow" class="w-full h-full object-cover" />
          </div>
          <div>
            <div class="text-white font-semibold text-xs">Personal system prompt</div>
            <div class="text-[10px] text-gray-400">Generated by Shadow</div>
          </div>
        </div>
        <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
        </svg>
      </div>

      <!-- Content -->
      <div class="px-4 py-4">
        <div class="space-y-3">
          <div class="text-base font-semibold text-white leading-snug">
            You're a Product Manager at Acme in charge of Ask AI, Agent Hub, Search, Analytics and Notifications. Each product line has a dedicated engineering team (5 engineering managers) and 3 POs.
          </div>
          <div class="text-base font-semibold text-white leading-snug">
            Your main goal this quarter is to ship Agent Hub, the new agentic experience for Acme. It has been announced and sold to early customers by your leadership, and you need to reach 80% accuracy level before beta launch.
          </div>
          <div class="text-base font-semibold text-white leading-snug">
            Shadow will act as your PM sidekick, helping you know <span class="text-purple-400">what to ship first</span> to reach your main goal.
          </div>
        </div>
      </div>

      <!-- Footer -->
      <div class="px-4 py-3 bg-[#35354a] border-t border-gray-700/50">
        <div class="flex items-center justify-between">
          <div class="text-[10px] text-gray-400">This prompt persists across every conversation</div>
          <button class="px-3 py-1.5 bg-purple-600 hover:bg-purple-700 text-white text-xs font-medium rounded-lg transition-colors">
            Save & Continue
          </button>
        </div>
      </div>
    </div>
  </div>
</div>
